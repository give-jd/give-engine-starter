# Dipendenze dell'ORCHESTRATOR / Cabina di Regia (processo locale).
#
# Installato SOLO dall'installer (install.sh/.ps1) e dall'auto-update.
# NON da Vercel: le serverless function in api/ usano solo httpx + stdlib, quindi
# fastapi/uvicorn/tiktoken/psutil qui NON devono finire nel bundle delle lambda
# (causavano il superamento del limite 250/500MB). Vedi requirements.txt (root)
# che resta minimale per Vercel.
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
websockets>=12.0
pyyaml>=6.0
rich>=13.7
python-dotenv>=1.0
httpx>=0.27
psutil>=5.9
tiktoken>=0.5
